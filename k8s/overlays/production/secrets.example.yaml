# TEMPLATE ONLY — dummy values. Copy to secrets.enc.yaml and encrypt:
#   cp secrets.example.yaml secrets.enc.yaml && sops --encrypt --in-place secrets.enc.yaml
# Generate strong values: `openssl rand -base64 32` (tokens/keys), ed25519 keypair for the
# account signing/verify keys, and your GitHub prod OAuth app credentials.
apiVersion: v1
kind: Secret
metadata:
  name: telos-secrets
  namespace: telosmud
type: Opaque
stringData:
  POSTGRES_PASSWORD: "CHANGE-ME"
  TELOS_POSTGRES_DSN: "postgres://telos:CHANGE-ME@postgres:5432/telosmud?sslmode=disable"
  TELOS_WEB_SESSION_KEY: "CHANGE-ME-random-32B"
  TELOS_ACCOUNT_CALLER_TOKEN: "CHANGE-ME-random-32B"
  TELOS_ACCOUNT_SIGNING_KEY: "CHANGE-ME-ed25519-private"
  TELOS_ACCOUNT_VERIFY_KEY: "CHANGE-ME-ed25519-public"
  TELOS_HANDOFF_SIGNING_KEY: "CHANGE-ME-ed25519-private"
  TELOS_HANDOFF_VERIFY_KEY: "CHANGE-ME-ed25519-public"
  TELOS_GITHUB_CLIENT_ID: "CHANGE-ME"
  TELOS_GITHUB_CLIENT_SECRET: "CHANGE-ME"
